# python bin function to convert the decimal value to the binary value
import os

value = int(input(" Please let me know the value for the binary"))
print( " this the binary value for the decimal number",value, bin(value))