import os
import sys 


maxIntergerVlaueOfSystem = sys.maxsize
minimumIntergerVlaueOfSystem = -sys.maxsize -1

print("this is the maximumvalue integer of the system", maxIntergerVlaueOfSystem)
print("this is the minimumvalue integer of the system", minimumIntergerVlaueOfSystem)