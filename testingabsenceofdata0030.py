import os
import sys
os.system("cls")

# we are working with if and else statement to know the branch controal
value = input("please enter a enter key strong")

# if this has data then this become true, so we did not gave any data
# in the python we do not need to give any condition.
if value:
	print(" the value of the programe is  having a value")
else:
	print("the value of the programe is negative")
	print("i am in the else block tracing it there is no value.")
    
print("this the block just to check main block of operation operated out of if loop")